server.port: 9090

spring:
  sql:
    init:
      mode: always
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/ewm-stats
    username: dbuser
    password: 12345
  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true
